# State

- Date: 2026-03-27
- Branch: `main`
- Task: Add a dex-aware `get_mark_price(coin)` path that returns live `markPx` values for primary perp, HIP-3 perp dex, and spot markets.
- Progress: Added `InfoAPI.get_perp_meta_ctx(dex="")` so perp asset-context requests can target a specific dex, introduced `AsyncHyperliquid.get_mark_price(coin)` in the internal info client to route spot coins through `get_spot_meta_ctx()` and perp coins through the appropriate `get_perp_meta_ctx(dex)`, and changed deprecated `get_market_price()` to delegate to the new single-coin live mark-price path. Root-cause debugging then found that spot `get_mark_price()` was reading `spotMetaAndAssetCtxs` by `universe` enumeration order instead of each pair's returned `index`, which misrouted some quoted spot pairs to the wrong `markPx`. Fixed the lookup to use the spot pair `index`, updated unit coverage to catch misaligned spot context ordering, and tightened live mainnet integration coverage with a dedicated session-scoped fixture that avoids `429` rate limits by reusing one initialized client. Live verification confirmed current mainnet symbols `xyz:SILVER` and `USDT0/USDC`, while `xyz:SLIVER` and `USDT/USDC` remain unsupported aliases that raise `ValueError`. Followed up by adding an explicit HYPE parity check across `HYPE` perp, `HYPE/USDC`, `HYPE/USDH`, `HYPE/USDT0`, and `@107`. Added a code-level TODO in `init_metas` to track future HIP-4 outcome meta/asset initialization, since outcome asset IDs use a separate encoding path from the current perp and spot offset schemes.
- Verification: `uv run pytest -q tests/unit/test_info_mark_price.py tests/unit/test_perf_hotpaths.py`, `uv run pytest -q tests/integration/test_info.py -k 'live_mainnet'`, `uv run ruff format`, `uv run ruff check`, and `uv run ty check` all passed on 2026-03-27. After the follow-up HIP-4 TODO comment was added, `uv run ruff format`, `uv run ruff check`, and `uv run ty check` were rerun and also passed on 2026-03-27. The live mainnet integration run now covers `BTC`, `xyz:NVDA`, `xyz:SILVER`, `PURR/USDC`, `HYPE/USDC`, `USDT0/USDC`, `USDE/USDC`, `USDH/USDC`, `@107`, negative checks for unsupported aliases `xyz:SLIVER` and `USDT/USDC`, and a HYPE parity assertion requiring `HYPE`, `HYPE/USDC`, `HYPE/USDH`, `HYPE/USDT0`, and `@107` to remain within `1%` or `0.05` absolute of the perp anchor.
